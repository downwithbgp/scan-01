# Scan 01 — packtool
"""The community-side toolchain: packs (JSON) <-> EEPROM bytes, plus the
k5prog UART protocol for dump/flash. Mirrors the firmware's settings_pack.c
byte-for-byte (the golden fixture test proves it)."""
